[paths]
csv = ""
build_dir = "/home/vishu/repos/hbmep-paper/reports/human/tms/fit/mixed-effects"

[vars]
subject = "participant"
features = ["intervention"]
intensity = "TMSInt"
response = ['PKPK_APB']

[mcmc]
chains = 4
warmup = 500
samples = 1000

[model]
link = "fixed"

[fixed]

# [optional-mep-data]
# mep_matrix_path = "/home/vishu/data/hbmep-processed/human/tms/mep_matrix.npy"    # In .npy format with shape: (No. of rows in csv) x (time) x (No. of responses)
# mep_response = ['ADM', 'APB', 'Biceps', 'ECR', 'FCR', 'Triceps']              # Order of responses in MEP matrix
# mep_time_range = [-0.15, 0.15]

# [optional-mep-size-window]
# mep_size_time_range = [0.015, 0.075]

[aesthetics]
base = 10
